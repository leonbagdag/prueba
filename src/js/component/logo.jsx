import React from 'react';
//import { FontAwesomeIcon } from '@fortawesome/react-fontawesome';

const Logo = (props) => {
	return <h1 className={`logo ${props.className}`}>Logo</h1>;
};

export default Logo;
